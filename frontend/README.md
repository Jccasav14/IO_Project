Frontend (React)

Run steps:
1) In one terminal:
   python lp_api_server.py

2) In another terminal:
   cd frontend
   npm install
   npm run dev

Then open the dev server URL and use the UI. It sends POST to
http://127.0.0.1:8000/solve and displays the result.
